{
  "name": "node-first-steps-typescript",
  "version": "1.0.0",
  "description": "---",
  "main": "app.js",
  "engines": {
    "node": "20.10.0"
  },
  "scripts": {
    "start": "npm run build && node dist/app.js",
    "dev": "nodemon",
    "watch": "node --watch src/app.ts",
    "build": "rimraf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "nodemon": "^3.1.4",
    "rimraf": "^5.0.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  }
}

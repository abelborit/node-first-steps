/* este código se podría ejecutar en un navegador o sino con alguna extensión, pero también podemos usar Node. Entonces, abrimos la terminal y colocamos "node app" (se podría colocar también "node app.js" pero como estamos en Node y Node ejecuta JavaScript entonces la extensión .js puede ser opcional) */
const message = "Hola Mundo";

console.log(message);
